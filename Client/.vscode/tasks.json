{
// See https://go.microsoft.com/fwlink/?LinkId=733558
// for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and flash", 
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "make TARGET=nrf52840 BOARD=dk client.upload", 
            "group": "build"
        }
    ]
}